#include <bits/stdc++.h>
using namespace std;
int main()
{
  int arr[51];
  int n,k,l,j,i;
  cin>>n>>k;
  i = sizeof(arr)/sizeof(arr[0]);

    sort(arr, arr+i, greater<int>());
  l=0;
  for(j=0;j<n;j++)
  {
    cin>>arr[j];

  }
  for(j=0;j<n;j++)
  {
    if(arr[j]>arr[k])
    {
      l=l+1;
    }
   
  }
  cout<<l;
  return 0;
}
